// This is a very simple example demonstrating basic operations with arrays.
// You can modify it to perform more complex tasks as needed.
const numbers: number[] = [1, 2, 3, 4, 5];

numbers.forEach((element) => {
  console.log(element);
});
